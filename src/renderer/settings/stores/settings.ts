export interface StreamCursorSettings {
  cursorStyle: string
  primaryColor: string
  strokeColor: string
  glowEnabled: boolean
  glowRadius: number
  cursorSize: number
  enabled: boolean
  hideSystemCursor: boolean
  toggleHotkey: string
  autostart: boolean
  startMinimized: boolean
  pollRate: number
}
